---
title: "Recon Mode"
---

Recon Mode provides read-only access to your Kubernetes cluster for both AI agents and human operators, creating a safe environment for exploration, audits, compliance checks, and troubleshooting without the risk of unintended modifications.

<Frame caption="Recon Mode interface showing read-only cluster exploration">
  <img src="/images/recon-mode.png" alt="Recon Mode interface displaying safe cluster exploration capabilities" />
</Frame>

## Invoke Recon Mode

To activate or deactivate recon mode for read-only access:

1. Open the Agentkube app and look for the toggle button on the top right
2. Or use the shortcut (macOS: `Command + /`; Windows/Linux: `Ctrl + /`)